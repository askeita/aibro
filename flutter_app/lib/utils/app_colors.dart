import 'package:flutter/material.dart';

/// Shared application color constants.
class AppColors {
  const AppColors._();

  /// Standard dark gray used for button labels and header text.
  static const Color darkGray = Color(0xFF636363);

  /// Light gray used for pill/back button backgrounds (was Colors.grey.shade200).
  static const Color lightGrayBackground = Color(0xFFEEEEEE);

  /// Medium gray used for help/description text (was Colors.grey[600]).
  static const Color mediumGrayText = Color(0xFF757575);

  /// Secondary text gray (was plain Colors.grey).
  static const Color secondaryTextGray = Color(0xFF9E9E9E);

  /// Darker gray used for human avatar/timer icons (was Colors.grey.shade700).
  static const Color darkGray700 = Color(0xFF616161);
}
